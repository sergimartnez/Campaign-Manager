<h1>Create a new Prototype</h1>
<%= form_for [@my_campaign, @my_prototype] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>
  
  <h3>Once created you will be able to attch content files to this prototype</h3>

  <%= f.button %>
<% end %>

